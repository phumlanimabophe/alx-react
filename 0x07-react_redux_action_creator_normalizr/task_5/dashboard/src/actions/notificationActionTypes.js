// Action type constant for marking a notification as read
export const MARK_AS_READ = 'MARK_AS_READ';

// Action type constant for setting the type filter
export const SET_TYPE_FILTER = 'SET_TYPE_FILTER';

// Enum-like object for notification type filters
export const NotificationTypeFilters = {
    DEFAULT: 'DEFAULT',
    URGENT: 'URGENT',
};
